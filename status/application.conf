# vim:set ft=tmux:
%hidden MODULE_NAME="application"

set -ogq "@ayu_${MODULE_NAME}_icon" " "
set -ogqF "@ayu_${MODULE_NAME}_color" "#{E:@thm_maroon}"
set -ogq "@ayu_${MODULE_NAME}_text" " #{pane_current_command}"

source -F "#{d:current_file}/../utils/status_module.conf"
